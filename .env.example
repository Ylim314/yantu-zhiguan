# 后端服务端口
PORT=3000

# JWT 签名密钥，正式部署时必须替换为随机长字符串
JWT_SECRET=please-change-this-secret-in-production

# JWT 有效期
JWT_EXPIRES_IN=7d

# SQLite 数据库路径，默认可留空使用 server/data/yantu.db
DATABASE_PATH=server/data/yantu.db

# 允许跨域访问的前端地址，多个地址使用英文逗号分隔
CLIENT_ORIGINS=http://localhost:5173
